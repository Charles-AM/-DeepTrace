# ⛔ DO NOT CITE — superseded by `../permanip_l2/`

Computed on L1 (leaky) checkpoints via `src/permanip.py`, which reloads models.

**Superseded, not merely flagged.** `../permanip_l2/` recomputes the same
breakdown from the committed video-disjoint prediction dumps — no GPU, no
checkpoints, and it cannot drift back onto the wrong protocol. The substantive
finding (frequency_only strongest on Deepfakes, weakest on NeuralTextures)
replicates there; the absolute AUCs do not.
