# ⛔ DO NOT CITE — computed on L1 (leaky) checkpoints

**Gap G7.** Same defect as `../late_fusion/`: `src/cka.py` reloads `best.pt`, and
these results came from the pre-protocol-fix `c40-run` output.

Supplementary in `../../docs/ESSENCE.md` §12, and under review for removal rather
than redoing — it would need additional controls and space without advancing the
evaluation-unit contribution.
