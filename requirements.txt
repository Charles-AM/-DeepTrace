# Core (Kaggle images already ship most of these; versions are lower bounds)
torch>=2.2
torchvision>=0.17
numpy>=1.24
scipy>=1.10

# Models / metrics / viz — added as later tasks need them
timm>=0.9
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0

# Dev
pytest>=7.4
